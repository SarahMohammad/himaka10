class FilterSelections{
  var lowerPrice;
  var upperPrice;
  var catId;
  int isUsed;
  List<int> ratings = [];

  FilterSelections(this.lowerPrice, this.upperPrice, this.catId, this.ratings,this.isUsed);
}